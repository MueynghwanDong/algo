String

해시테이블을 만들면 검색 속도를 향상시킬 수 있다.
탐색 키를 매개변수로 넣으면 주소값(해시 주소) -> 해당 주소에 값을 넣어줌.

충돌 (서로 다른 값이 동일 해시 주소를 가리키는 경우)
-> 충돌 해결 방법 
- 개방주소법
- 체이닝

해시주소를 얻을 수 있느 메서드 -> hashcode 메서드
적절한 해시 값이 맞는지 equals 메서드
-> 두 메서드가 오버로딩 되어 있어야 함.

Wrapper / String / Object 클래스들은이미 오버로딩 되어있다.

re trie val

trie -> 문자열 집합을 표현하는 트리
- 모든 문자열은 다른 문자열의 접두어가 아니라 가정

 ex) banana
 모든 접미사  	정렬			lcp 	모든 접두사를 구하기.. -> 모든 접두사 값도 정렬된 형태가 됨..
 6 a		6 a			0		-> 중복된은 값은 제거해주기 
 5 na		4 na		1
 4 ana		2 anana		3
 3 nana		1 banana	0
 2 anana	5 na		0
 1 banana	3 nana		2